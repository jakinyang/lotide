const assertEqual = require('../assertEqual')
/* 
assertEqual(1, 1);      // => Passes
assertEqual("a", "a");  // => Passes
assertEqual(1, 2);      // => Fails
assertEqual("a", "b");  // => Fails
  */